# crave.io credentials — fill via GitHub Secrets
# Download your real crave.conf from https://foss.crave.io/API -> API Keys
# Then create secrets CRAVE_USERNAME and CRAVE_TOKEN from it
[Crave]
username = ${CRAVE_USERNAME}
token = ${CRAVE_TOKEN}
